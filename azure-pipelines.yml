trigger:
  branches:
    include: [ "*" ]
  tags:
    include: [ "*" ]

pool:
  name: WS

resources:
  repositories:
  - repository: AZ-CI
    type: git
    name: AZ-CI
    
stages: 
- stage: CodeQuality Stage
  displayName: 'Checking the Code Quailty Stage'
  jobs:
  - template: "codeCheck.yml@AZ"