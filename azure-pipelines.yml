trigger:
  branches:
    include: [ "*" ]
  tags:
    include: [ "*" ]

pool:
  name: WS

resources:
  repositories:
  - repository: azureCI
    type: git
    name: azureCI
    
stages: 
- stage: CodeQuality Stage
  displayName: 'Checking the Code Quailty Stage'
  jobs:
  - template: "codeCheck.yml@AZ"