trigger:
  branches:
    include: [ "*" ]
  #tags:
  #  include: [ "*" ]

pool:
  name: WS

resources:
  repositories:
  - repository: azureCI
    type: git
    name: azureCI
    
variables:
  COMPONENT: cart
    
stages: 
- template: "codecheck.yml@azureCI"
  
  